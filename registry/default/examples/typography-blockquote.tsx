'use client';

import { TypographyBlockquote } from '@/registry/default/ui/typography';

export default function TypographyBlockquoteExample() {
  return (
    <TypographyBlockquote>
      Every financial movement is a transaction. Every transaction tells a story
      about how money flows through your system.
    </TypographyBlockquote>
  );
}
