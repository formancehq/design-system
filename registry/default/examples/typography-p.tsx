'use client';

import { TypographyP } from '@/registry/default/ui/typography';

export default function TypographyPExample() {
  return (
    <TypographyP>
      Each ledger maintains an immutable log of all transactions posted against
      it. Accounts are created on the fly and balances are computed in real time
      from the transaction history.
    </TypographyP>
  );
}
