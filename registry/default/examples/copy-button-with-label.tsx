import { CopyButton } from '@/registry/default/ui-fragments/copy-button';

export default function CopyButtonWithLabel() {
  return <CopyButton text="npx @formance/ds init --internal" label="Copy" />;
}
